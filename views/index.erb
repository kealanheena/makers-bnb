<h1>Makers BnB</h1>
<% if @user %>
  <a href='/new' class="btn btn-primary">Add Rental</a>
<% end %>

<ul>
  <% @list.each do |rental| %>
    <li>Name: <% if @user %><a href="/rental/<%= rental.id %>"><% end %><%= rental.name %></a></li>
    <li>Description: <%= rental.description %></li>
    <li>Price Per Night: £<%= rental.price %></li>
    <li>Start Date: <%= rental.starting.strftime('%d %B %Y') %>
    <li>End Date: <%= rental.ending.strftime('%d %B %Y') %><br>
  <% end %>
</ul>
